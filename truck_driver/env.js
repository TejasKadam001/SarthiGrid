// Environment Configuration for Mobile App WebView
window.ENV = {
  // Python backend status & approval API server address
  // When running on a real mobile device, change "localhost" to your computer's local IP address (e.g., "http://192.168.1.100:5005")
  API_BASE_URL: "http://localhost:5005"
};
